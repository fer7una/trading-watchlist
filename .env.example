# --- Required ---
FMP_API_KEY=put_your_key_here
FLOAT_ALLOW_STALE_DAYS=14

# --- IBKR API connection ---
# If TWS/IB Gateway runs on Windows, WSL often reaches it via 127.0.0.1 (localhost forwarding).
# If that fails, set IB_HOST to your Windows host IP (see README).
IB_HOST=127.0.0.1
IB_PORT=7497
IB_CLIENT_ID=7

# --- RVOL calc ---
# 04:00 for premarket-style, 09:30 for regular session.
RVOL_ANCHOR_NY=04:00
RVOL_LOOKBACK_DAYS=20
# 0 includes extended hours, 1 only regular session
USE_RTH=0

# --- Profile selection / market phase ---
PROFILE=auto
MARKET_CALENDAR=NYSE
CLOSED_FALLBACK=last_ok
CLOSED_STALE_MAX_HOURS=36
REQUIRE_ACTIVE_MARKETDATA=0
# set to 1 only if you want to force PROFILE=open outside session
FORCE_PROFILE=0

# --- Filters (Ross-ish) ---
PRICE_MIN=2
PRICE_MAX=20
CHANGE_MIN_PCT=10
VOLUME_MIN=200000
RVOL_MIN=3
FLOAT_MAX=10000000
SPREAD_MAX=0.15
MAX_CANDIDATES=50
MAX_RVOL_SYMBOLS=30

# --- News / catalyst (FMP) ---
# Set to "none" to disable.
NEWS_PROVIDER=fmp
# 0 disables news lookup.
NEWS_LOOKBACK_HOURS=24
# limit debug output; set to 0 to disable.
NEWS_DEBUG_TOP_N=10

# --- Profile overrides (optional) ---
# Premarket profile (PRE_)
# PRE_PRICE_MIN=2
# PRE_PRICE_MAX=20
# PRE_FLOAT_MAX=10000000
# PRE_CHANGE_MIN_PCT=10
# PRE_VOLUME_MIN=200000
# PRE_RVOL_MIN=3
# PRE_RVOL_ANCHOR_NY=04:00
# PRE_USE_RTH=0
# PRE_SPREAD_MAX=0.15
# PRE_MAX_CANDIDATES=50
# PRE_MAX_RVOL_SYMBOLS=30
#
# Open profile (OPEN_)
# OPEN_PRICE_MIN=2
# OPEN_PRICE_MAX=20
# OPEN_FLOAT_MAX=10000000
# OPEN_CHANGE_MIN_PCT=10
# OPEN_VOLUME_MIN=200000
# OPEN_RVOL_MIN=3
# OPEN_RVOL_ANCHOR_NY=09:30
# OPEN_USE_RTH=1
# OPEN_SPREAD_MAX=0.15
# OPEN_MAX_CANDIDATES=50
# OPEN_MAX_RVOL_SYMBOLS=30

# --- Paths ---
WATCHLIST_DB=./data/watchlist.db
OUT_DIR=./out
